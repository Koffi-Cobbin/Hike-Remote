'use client'; import {useState} from 'react'; import Link from 'next/link'; import {Menu, X, ArrowUpRight} from 'lucide-react';
const links=[['Adventures','/adventures'],['Experiences','/experiences'],['Community','/community'],['Stories','/stories'],['Corporate','/corporate'],['About','/about']];
export default function Header(){const[open,setOpen]=useState(false);return <header className="absolute top-0 z-50 w-full text-white"><div className="container flex h-20 items-center justify-between"><Link href="/" className="display text-2xl font-bold tracking-tight">HIKE<span className="text-[#e37a48]">.</span>REMOTE</Link><nav className="hidden items-center gap-7 lg:flex">{links.map(([n,h])=><Link className="text-sm font-semibold text-white/85 hover:text-white" href={h} key={h}>{n}</Link>)}</nav><div className="hidden lg:block"><Link href="/adventures" className="btn btn-primary">Join the movement <ArrowUpRight size={16}/></Link></div><button aria-label="Open menu" className="lg:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="absolute left-0 top-20 w-full border-t border-white/20 bg-[#183c2c] p-6 lg:hidden"><nav className="flex flex-col gap-5">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} className="text-lg font-semibold" href={h} key={h}>{n}</Link>)}<Link href="/adventures" className="btn btn-primary w-fit">Join the movement <ArrowUpRight size={16}/></Link></nav></div>}</header>}
