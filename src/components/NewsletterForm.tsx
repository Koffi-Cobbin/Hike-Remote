'use client'; import {useState} from 'react'; export default function NewsletterForm(){const[email,setEmail]=useState('');const[done,setDone]=useState(false);return done?<p className="rounded-xl bg-white/10 p-4 text-sm text-white">You’re on the list. See you outside.</p>:<form onSubmit={e=>{e.preventDefault();if(email)setDone(true)}} className="flex max-w-md gap-2"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address" aria-label="Email address" className="min-w-0 flex-1 rounded-full bg-white px-5 py-3 text-sm text-[#18251d] outline-none"/><button className="btn btn-primary">Join us</button></form>}
