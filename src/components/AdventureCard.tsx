import Link from 'next/link'; import Image from 'next/image'; import {MapPin, CalendarDays, ArrowUpRight} from 'lucide-react'; import type {Adventure} from '@/lib/data'; export default function AdventureCard({a}:{a:Adventure}){return <article className="group overflow-hidden rounded-2xl bg-white shadow-sm"><Link href={'/adventures/'+a.slug}><div className="relative h-64 overflow-hidden"><Image className="card-image object-cover" src={a.image} alt={a.title} fill sizes="(max-width: 700px) 100vw, 33vw"/><div className="absolute left-4 top-4 rounded-full bg-[#f5f2ea] px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-[#1e4b36]">{a.type}</div></div><div className="p-5"><div className="flex items-center justify-between"><span className="eyebrow">{a.date}</span><ArrowUpRight size={18} className="text-[#d96d3b]"/></div><h3 className="display mt-3 text-2xl font-bold">{a.title}</h3><div className="mt-4 flex flex-wrap gap-3 text-xs text-[#687268]"><span className="flex items-center gap-1"><MapPin size={13}/>{a.location}</span><span>·</span><span>{a.difficulty}</span><span>·</span><span>{a.duration}</span></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-[#687268]">{a.description}</p></div></Link></article>}
