import {notFound} from 'next/navigation'; import Image from 'next/image'; import {stories} from '@/lib/data'; export function generateStaticParams(){return stories.map(s=>({slug:s.slug}))} export default async function Story({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=stories.find(x=>x.slug===slug);if(!s)notFound();return <main className="pt-20"><section className="bg-[#173c2c] py-20 text-white"><div className="container max-w-4xl"><p className="eyebrow text-[#f2a06c]">{s.category} · {s.date}</p><h1 className="display mt-5 text-5xl font-bold md:text-7xl">{s.title}</h1><p className="mt-6 text-lg text-white/70">{s.excerpt}</p></div></section><article className="container max-w-3xl py-16"><Image src={s.image} alt={s.title} width={1200} height={700} className="h-[420px] w-full rounded-2xl object-cover"/><p className="mt-10 text-lg leading-9 text-[#49574c]">There are places that ask you to arrive with less urgency. To notice the change in the air, the sound of your own footsteps, and the person walking beside you. This is one of those stories — a reminder that the best part of going somewhere new is often who you become along the way.</p><p className="mt-6 text-lg leading-9 text-[#49574c]">At Hike Remote, every trail is an invitation. Not just to see more, but to feel more connected: to the land, to the moment, and to the people who share it with you.</p></article></main>}
